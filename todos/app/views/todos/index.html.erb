<%= link_to 'Create a Todo', new_todo_path %>

<ul id="my-todos">
	<% @todos.each do |todo| %>
		<li id="<%= dom_id todo %>" class="<%= todo_state todo %>">
			<%= todo.description %>
			<%= link_to "Complete", todo_completion_path(todo), method: :post %>
		</li>
	<% end %>
</ul>